Add input validation to the sample application so invalid names are rejected.
Success means existing tests pass and a unit test proves empty names fail.
Only modify files within examples/app and do not add runtime dependencies.
